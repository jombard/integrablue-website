# Email Configuration
# You'll need to set up an email service (Gmail, SendGrid, etc.) and get these credentials

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Where to send contact form emails
CONTACT_EMAIL=hello@integrablue.com

# Optional: For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" (not your regular password)
# 3. Use that app password as SMTP_PASS 